{
  "name": "duoquest",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:migrate": "pnpm --filter @duoquest/api prisma migrate dev",
    "db:push": "pnpm --filter @duoquest/api prisma db push",
    "db:generate": "pnpm --filter @duoquest/api prisma generate",
    "db:studio": "pnpm --filter @duoquest/api prisma studio",
    "app:run:android": "pnpm --filter @duoquest/web run cap:run:android",
    "app:run:android:reload": "pnpm --filter @duoquest/web run cap:run:android:reload",
    "app:build:release": "pnpm --filter @duoquest/web run cap:build:release"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0"
}
